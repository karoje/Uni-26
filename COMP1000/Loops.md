A loop will check the expression again after every iteration
```java
while (boolean expression) {

    code inside loop

}

rest of the code
```
For-loop is more compact and more commonly used
```java
for (initializations; boolean expression; update(s)) {

    code inside loop;

}

rest of the code
```
